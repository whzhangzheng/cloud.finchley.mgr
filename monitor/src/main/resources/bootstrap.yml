spring:
  profiles:
    active: dev

logging:
  config: classpath:logback-spring.xml
  level:
    root: info

management:
  endpoints:
    web:
      exposure:
        include: "*"
        #exclude: []
